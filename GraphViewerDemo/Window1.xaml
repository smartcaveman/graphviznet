<Window x:Class="GraphViewerDemo.Window1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Window1" Height="600" Width="600" xmlns:graph="clr-namespace:GraphViewer;assembly=GraphViewer"
        xmlns:local="clr-namespace:GraphViewerDemo">
    <Window.Resources>
        <DataTemplate DataType="{x:Type local:LetterNode}">
            <Border BorderThickness="2" BorderBrush="Red" CornerRadius="3">
                <TextBlock Text="{Binding Name}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBox Grid.Column="0" Grid.Row="0" Text="{Binding Source, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <Button Grid.Column="1" Grid.Row="0" Content="Refresh" Command="{Binding RefreshCommand}"/>
        <ScrollViewer Grid.Row="1" Grid.ColumnSpan="2">
            <graph:GraphViewerSlim Graph="{Binding Graph}" x:Name="graph"/>
        </ScrollViewer>
    </Grid>
</Window>
